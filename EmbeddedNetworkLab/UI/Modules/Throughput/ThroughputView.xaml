<UserControl x:Class="EmbeddedNetworkLab.UI.Modules.Throughput.ThroughputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmbeddedNetworkLab.UI.Modules.Throughput"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    
    <Grid Margin="20">
        <StackPanel>

            <TextBlock Text="Throughput Test"
                   FontSize="18"
                   Margin="0,0,0,15"/>

            <TextBlock Text="{Binding CurrentRate, StringFormat=Throughput: {0:F2} Mbps}"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

            <Button Content="{Binding StartStopLabel}"
                Command="{Binding ToggleCommand}"
                Width="120"
                Height="30"/>

        </StackPanel>
    </Grid>
</UserControl>
