<UserControl x:Class="EmbeddedNetworkLab.UI.Modules.Serial.SerialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header: title, port selector, baudrate selector, open/close -->
		<StackPanel Orientation="Horizontal" Margin="4">
			<TextBlock Text="{Binding Title}"
					   FontWeight="Bold"
					   VerticalAlignment="Center"/>

			<!-- Serial port selector -->
			<StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
				<TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,6,0"/>
				<ComboBox ItemsSource="{Binding SerialPorts}"
						  SelectedItem="{Binding SelectedPort, Mode=TwoWay}"
						  Width="80"
						  VerticalAlignment="Center"
						  IsEditable="False"/>
				<Button Command="{Binding RefreshPortsCommand}"
						Margin="6,0,0,0"
						Padding="4"
						ToolTip="Refresh available ports"
						VerticalAlignment="Center">
					<TextBlock FontFamily="Segoe MDL2 Assets"
							   Text="&#xE149;"
							   FontSize="14"/>
				</Button>
			</StackPanel>

			<!-- Baudrate selector -->
			<StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
				<TextBlock Text="Baudrate:" VerticalAlignment="Center" Margin="0,0,6,0"/>
				<ComboBox ItemsSource="{Binding BaudRates}"
						  SelectedItem="{Binding SelectedBaud, Mode=TwoWay}"
						  Width="80"
						  IsReadOnly="False"
						  VerticalAlignment="Center"/>
			</StackPanel>

			<Button Content="{Binding TogglePortButtonText}"
					Command="{Binding TogglePortCommand}"
					Margin="12,0,0,0"
					Padding="12,4"
					VerticalAlignment="Center"
					MinWidth="60"/>
		</StackPanel>

		<TextBox Grid.Row="1"
				 Text="{Binding SerialText}"
				 FontFamily="Consolas"
				 FontSize="12"
				 IsReadOnly="True"
				 VerticalScrollBarVisibility="Auto"
				 TextWrapping="Wrap"
				 Margin="4"/>
	</Grid>
</UserControl>